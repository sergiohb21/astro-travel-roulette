---
const {img, alt, title, text, href, linkText} = Astro.props;
export interface Props {
  img: string;
  alt: string;
  title: string;
  text: string;
  href?: string;
  linkText?: string;
}
---
<div class="col">
<div class="card" style="width: 18rem;">
  <img src={img} class="card-img-top img-fluid" alt={alt} title={alt}>
  <div class="card-body">
    <h5 class="card-title">{title}</h5>
    <p class="card-text">{text}</p>
    <a href={href} class="btn btn-primary">{linkText}</a>
  </div>
</div>
</div>

